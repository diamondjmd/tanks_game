#pragma once

#include <array>
#include <unordered_map>
#include "Point.h"

namespace controls{
    enum Action {
        Right,
        Down,
        Left,
        Up,
        Fire,
        Close,
        Count
    };
} // namespace engine